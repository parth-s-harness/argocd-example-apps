wordpress:
  image:
#    tag: invalid #break this app on purpose until a dedicated broken app is created
    tag: latest 
  wordpressPassword: foo
  mariadb:
    db:
      password: bar
    rootUser:
      password: baz
